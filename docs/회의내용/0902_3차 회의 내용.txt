<09.02 3차 회의 내용>

[2차 plan.md 수정 사항]
24. Frontend Flow
카테고리 선택 -> 내 사례 분석 -> 법 검색, 실제 사례, 쉬운 법률 용어, 필요 서류, 다음 행동, FAQ, 질의 이력
이 순서대로.

27. 역할 분담
DB의 경우 오픈 API 키는 MCP 서버에서 사용할 때마다 키를 통해서 Redis처럼 일정기간이 지나면 다시 연결을 해서 데이터를 
가져와야 되는지 아니면 DB에 먼저 저장한 상태에서 해야 되는 건지 정해야 함.
오픈 API 키 팀 DB에 영속적으로 저장이 되는지 확인 필요.

[이번주 금요일까지 정해야 하는 것]
- DB 자료 구조 정의
- 스키마
- 오픈 API 키 팀 DB에 영속적으로 저장이 되는지 확인 필요
- 데이터 소스 어디에서 얻을 수 있을지(소스가 법원 사이트 오픈 API 키 하나밖에 없음)
- MCP 서버 Tool 기능 정의(대략적으로) 
* 이번주 금요일까지는 다 정해두고 해당 사항 담아서 최종본 md로 만들어야 함.

[추가로 얘기 나온 것]
- 프론트엔드 디자인 필요함.
- DB는 오픈 API 키 사용 전에 mock 데이터 먼저 만들어보고 테스트해보기
- DB -> MCP 서버 -> AI Agent -> 프론트엔드. 중요도 순서.